# @package _global_
outcome:
  freeze: true
treatment:
  loss: telescope
  norm_weight: 0.0
tr:
  freeze: true
  weight: 0.0
training:
  finetune:
    freeze_nuisance: false
    after: 1.0  # never
body:
  independent_encoders: true
estimator: ipw
# best_metric: val/ratio
